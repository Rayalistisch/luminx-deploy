#!/usr/bin/env node
import { ExitCode } from '../index.js';

// Placeholder entry point. The command registry, argument parsing and rendering land in M3.
process.stdout.write('luminx 0.0.0 — scaffolding. Commands land in M3.\n');
process.exit(ExitCode.Success);
