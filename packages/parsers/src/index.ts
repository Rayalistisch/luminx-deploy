/**
 * @luminx/parsers — file readers that answer questions about a project.
 *
 * A parser returns a parsed fact or null. It never guesses. Unlike core, this package
 * is allowed to name concrete CMSes: identifying them is its job.
 *
 * Contents land in M4. See docs/architecture.md §3.3.
 */
export {};
